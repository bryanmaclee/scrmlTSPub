// 02a — Iteration. for/lift inside ${} inside <ul>.

<program>

${
  @items = ["apple", "banana", "cherry"]
}

<ul>
  ${
    for (let x of @items) {
      lift <li>${x}</li>
    }
  }
</ul>

</program>
