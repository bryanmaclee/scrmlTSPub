// control-002-if-else.scrml
// If/else conditional. Tests: if/else branches in logic.

<div>
    ${
        let score = 75
        if (score >= 60) {
            let result = "pass"
        } else {
            let result = "fail"
        }
    }
    <p>Score evaluated</>
</div>
