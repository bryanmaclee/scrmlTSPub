// control-008-for-in-markup.scrml
// For loop with lift to produce markup. Tests: lift inside for.

<ul>
    ${
        let fruits = ["apple", "banana", "cherry"]
        for (let f of fruits) {
            lift <li>${f}</>
        }
    }
</ul>
