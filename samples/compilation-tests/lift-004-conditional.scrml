// lift-004-conditional.scrml
// Lift with conditional logic. Tests: if inside for with lift.

<ul>
    ${
        let numbers = [1, 2, 3, 4, 5, 6, 7, 8]
        for (let n of numbers) {
            if (n % 2 == 0) {
                lift <li>Even: ${n}</>
            }
        }
    }
</ul>
