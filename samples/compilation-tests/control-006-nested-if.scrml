// control-006-nested-if.scrml
// Nested conditionals. Tests: if inside if.

<div>
    ${
        let x = 10
        let y = 20
        if (x > 5) {
            if (y > 15) {
                let result = "both"
            }
        }
    }
    <p>Nested check done</>
</div>
