// logic-012-nested-objects.scrml
// Nested objects and arrays. Tests: complex data structures.

<div>
    ${
        let config = {
            db: { host: "localhost", port: 5432 },
            features: ["auth", "api", "admin"]
        }
    }
    <p>Config loaded</>
</div>
