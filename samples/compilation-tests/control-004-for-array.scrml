// control-004-for-array.scrml
// For loop over an array. Tests: for...of in logic.

<div>
    ${
        let colors = ["red", "green", "blue"]
        for (let c of colors) {
            let upper = c.toUpperCase()
        }
    }
    <p>Colors processed</>
</div>
