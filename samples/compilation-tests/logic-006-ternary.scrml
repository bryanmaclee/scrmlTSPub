// logic-006-ternary.scrml
// Ternary expressions. Tests: condition ? a : b inside logic.

<div>
    ${
        let score = 85
        let grade = score >= 90 ? "A" : score >= 80 ? "B" : "C"
    }
    <p>Grade assigned</>
</div>
