// test-002-with-logic.scrml
// Test block with reactive vars. Tests: @reactive in test context.

@counter = 0

${
    function increment() {
        @counter = @counter + 1
    }
}

<p>${@counter}</>

~{ "logic tests"
    test "counter starts at zero" { assert @counter == 0 }
}
